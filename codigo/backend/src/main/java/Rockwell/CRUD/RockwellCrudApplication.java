package Rockwell.CRUD;

// Importação da classe principal do Spring Boot
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Anotação que marca a classe como uma classe de aplicação Spring Boot
@SpringBootApplication
public class RockwellCrudApplication {

    // Método principal que inicia a aplicação Spring Boot
    public static void main(String[] args) {
        SpringApplication.run(RockwellCrudApplication.class, args);
    }

}